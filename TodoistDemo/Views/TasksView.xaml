<Page
    x:Class="TodoistDemo.Views.TasksView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TodoistDemo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="ContentGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBox x:Name="AuthToken" HorizontalAlignment="Left" MinWidth="300" Header="Authentication token"></TextBox>
                    <AppBarButton x:Name="Sync" Icon="Save" Foreground="Black" VerticalAlignment="Center" Margin="0 30 0 0"></AppBarButton>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
